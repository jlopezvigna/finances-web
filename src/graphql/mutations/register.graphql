mutation Register($email: String!, $password: String!) {
  register(credentials: { email: $email, password: $password }) {
    errors {
      field
      message
    }
    user {
      ...RegularUser
    }
  }
}
